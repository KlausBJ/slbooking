<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :navn %><br>
    <%= f.text_field :navn %>
  </div>
  <div class="field">
    <%= f.label :beskrivelse %><br>
    <%= f.text_area :beskrivelse %>
  </div>
  <div class="field">
    <%= f.label :antal %><br>
    <%= f.number_field :antal %>
  </div>
  <div class="field">
    <%= f.label :tid %><br>
    <%= f.datetime_select :tid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
